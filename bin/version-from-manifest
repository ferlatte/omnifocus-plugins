#! /usr/bin/env python3

import json

def main() -> None:
    with open('./FerlatteTools.omnifocusjs/manifest.json') as manifestFile:
         manifest = json.load(manifestFile)
         print(manifest['version'])

if __name__ == "__main__":
    main()
